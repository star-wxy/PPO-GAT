seed: 42
total_timesteps: 300000
learning_rate: 0.00015
n_steps: 1024
batch_size: 256
gamma: 0.99
gae_lambda: 0.95
clip_range: 0.2
ent_coef: 0.003
vf_coef: 0.5
max_grad_norm: 0.5
n_epochs: 10
device: "auto"
checkpoint_dir: "outputs/checkpoints"
tensorboard_log: "outputs/tensorboard_naive"
best_model_dir: "outputs/best_model_naive"
eval_log_path: "outputs/eval_naive"
eval_freq: 5000
n_eval_episodes: 10
model_name: "ppo_gat_naive"

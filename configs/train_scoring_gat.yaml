seed: 42
total_timesteps: 500000
learning_rate: 0.00008
n_steps: 2048
batch_size: 512
gamma: 0.99
gae_lambda: 0.95
clip_range: 0.12
ent_coef: 0.001
vf_coef: 0.5
max_grad_norm: 0.5
n_epochs: 12
device: "auto"
checkpoint_dir: "outputs/checkpoints"
tensorboard_log: "outputs/tensorboard_scoring"
best_model_dir: "outputs/best_model_scoring"
eval_log_path: "outputs/eval_scoring"
eval_freq: 5000
n_eval_episodes: 10
model_name: "ppo_gat_scoring"
